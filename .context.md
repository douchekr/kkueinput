## 현재 상태
- [2026-03-16] 백엔드 명시 필수로 변경: `--tty`(TIOCSTI), `--tmux=SESSION`(tmux), 인자 없으면 help 출력
- [2026-03-16] 멀티라인 토글 구현 중 (테스트 단계)
  - 싱글: entry 내 ⊕ 아이콘으로 확장, ⌨ 아이콘 드래그/메뉴
  - 멀티: GtkTextView 5줄, Enter=줄바꿈, Ctrl+Enter=전송
  - 외부 - 버튼 + grip 아이콘(드래그/메뉴)은 멀티라인에서만 표시
  - CSS: textview 반투명 이중 레이어 문제 해결 (textview=transparent, textview text=rgba)
  - GTK3 no_show_all 주의: show_all 후 hide 패턴 사용

## 다음 목표
- [x] 아이콘 위치 스왑: PRIMARY=⌨(드래그/메뉴), SECONDARY=⊕(확장)
- [ ] ⊕ 아이콘 삭제, ⌨ 아이콘 하나로 통합
  - 좌클릭 -> 확장
  - 드래그 -> 이동
  - 우클릭 -> Context

## 핵심 메모
- GTK3 `no_show_all` 함정: 해당 위젯에 `show_all` 직접 호출해도 안 먹힘. `show_all`로 전체 realize 후 `hide`하는 패턴 사용
- 참고:
